# Current Benchmark Reproduction Gate

Time: 2026-06-18T21:31:15+08:00

## Scope

Added a gate that validates benchmark metrics generated by the current code, not only the archived reportable artifact. Docker was not started.

## Core Changes

- Added `scripts/verify-benchmark.ps1`.
- Updated `scripts/verify.ps1` so the benchmark step now runs `verify-benchmark.ps1` instead of discarding raw benchmark output.
- Updated `scripts/verify-release-files.ps1` to require `scripts/verify-benchmark.ps1`.
- Updated `README.md`, `docs/工程交付说明.md`, and `docs/实验说明.md` to distinguish:
  - `verify-benchmark.ps1`: regenerate a temporary benchmark from current code and validate metrics.
  - `verify-results.ps1`: validate archived reportable artifacts under `results/`.

## Gate Behavior

`verify-benchmark.ps1`:

- Runs `run_full_benchmark(Path.cwd(), repetitions=10, pressure_iterations=200)` using the root venv.
- Writes a temporary `benchmark.json` under `tmp-runtime/benchmark_verify_<timestamp>/`.
- Reuses `verify-results.ps1 -ResultDir <temp-dir>` for the same threshold logic.
- Deletes the temporary directory after validation.

## Verified Evidence

- `.\scripts\verify-benchmark.ps1` passed.
- All `scripts/*.ps1` files passed PowerShell syntax parsing.
- `.\scripts\verify-release-files.ps1` passed.
- `.\scripts\verify.ps1 -SkipFrontend` passed:
  - backend pytest 28/28 passed
  - backend compile check passed
  - baseline evaluation passed
  - current benchmark metric gate passed
- `git -c core.excludesFile= diff --check` passed.
- No Git-visible files remained under `tmp-runtime`.

## Remaining Boundary

Docker runtime verification remains intentionally unverified. Full completion still requires `.\scripts\final-audit.ps1` without `-SkipDocker`, unless the accepted scope explicitly excludes Docker runtime verification.
